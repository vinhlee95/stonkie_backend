{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "playwright install --with-deps chromium && celery -A celery_app worker --loglevel=info"
  }
}
